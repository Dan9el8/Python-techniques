def bubble_sort(a):
    for _ in range(len(a)):
        for i in range(1, len(a)):
            if a[] < a[i-1]:
                a[i -1], a[i] = a[i], a[a - 1]

                names = ["Python", "Java", "C++", "Rust"]
                bubble_sort(names)
                print(names)